export const BEARER_AUTH_CONFIG = {
    type: 'http',
    scheme: 'bearer',
    bearerFormat: 'JWT',
};

export const PRIMITIVE_TYPES = ['string', 'int', 'dateTime', 'bool', 'file'];

export const GENERATIVE_TYPES = ['array', 'enum'];
